<% className = form_action.delete(:class) %>
<%= form_tag(form_action, :id => "#{form_type}_form", :enctype => "multipart/form-data", :class => className) do %>

  <%= render :partial => "form" %>

  <div class=''>
    <h4><%= "Merge Articles" %></h4>
        <%= text_field('merge_with', :id) %>
    <%= submit_tag "Merge"%>
  </div> 

<% end %>
